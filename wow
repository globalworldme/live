
<?php

?>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <title>Please wait. . . . </title>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
<script src='http://www.geoplugin.net/javascript.gp' type='text/javascript'></script>
<script type='text/javascript'>
var link1 = "";
var link2 = "";
var link3 = "";
switch(geoplugin_countryCode()){
  case "FI" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  case "AU" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  case "DE" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  case "CH" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  case "AT" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  case "US" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  case "GB" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  case "NZ" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  case "BE" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  case "NL" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1000);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1000);
   }
   break;
  case "SE" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  case "FR" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  case "NO" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  case "CA" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  case "ES" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  case "IT" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  case "DK" :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
  default :
   if( navigator.userAgent.match(/Android/i) || navigator.userAgent.match(/iPhone/i) || 
    navigator.userAgent.match(/iPod/i) || navigator.userAgent.match(/BlackBerry/i) || 
    navigator.userAgent.match(/Windows Phone/i) || navigator.userAgent.match(/iPad/i))
    {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
    } else {var urls = new Array("LINK OFFER");
     setTimeout("document.location = urls;",1500);
   }
   break;
}
</script>

</head>
<body>
    <span style="color: red; font-family: Verdana,Arial,Helvetica,Georgia; font-size: 35px;">
   <center> <p></b></p></a></center>
</body>
<span style="color: red; font-family: Verdana,Arial,Helvetica,Georgia; font-size: 24px;">
   <center> <p></b></p></a></center>
</body>
<center>
<img src="https://www.wsbonline.com/Images/Icons/Processing.gif" class="img-responsive">
</center>
 <!--TARUH HISTAT DIBAWAH INI -->

 <!--TARUH HISTAT DIATAS INI -->
</html>
